

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/calendar_test}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:dlrlqor256}

spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration

# 테스트 환경 설정
spring.jpa.hibernate.ddl-auto=create-drop
logging.level.org.springframework.test=DEBUG

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

# Swagger 테스트환경 설정 (비활성화)
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

# Gmail SMTP 개발환경
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=leegeebark@gmail.com
spring.mail.password=etet iwwp tgis wjla
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

